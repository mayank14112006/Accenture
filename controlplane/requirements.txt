# ControlPlane core — deliberately light. Deterministic + lexical detectors only.
# Model-backed detector adapters are optional: see requirements-models.txt
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
PyYAML==6.0.2
httpx==0.28.1
pytest==8.*
